# QDUC Multi-State Configuration File
# 
# This file defines the nuclear states to calculate in a single run.
# Each line represents one state calculation (416 KDUQ parameter sets per state).
#
# FORMAT (pipe-separated):
# state_id | Q_value | l_transfer | j_transfer | nodes | initial_spin | final_spin | target_mass | target_Z | beam_energy | description
#
# PARAMETERS:
#   state_id     : Unique identifier for this state (integer)
#   Q_value      : Reaction Q-value in MeV
#   l_transfer   : Orbital angular momentum transfer (integer)
#   j_transfer   : Total angular momentum transfer (half-integer)
#   nodes        : Number of radial nodes in wave function (usually 0)
#   initial_spin : Target spin in incoming channel (half-integer, e.g., 2.5 for O19)
#   final_spin   : Target spin in outgoing channel (half-integer, e.g., 0.0 for O20 g.s.)
#   target_mass  : Mass number of target nucleus
#   target_Z     : Proton number of target nucleus
#   beam_energy  : Beam energy in MeV (total, not per nucleon)
#   description  : Human-readable description (no spaces, use underscores)
#
# Lines starting with # are comments and will be ignored.

# STATE 1 - Current O19 ground state configuration
1 | 5.383 | 2 | 2.5 | 0 | 2.5 | 0.0 | 19 | 8 | 8.0 | O19_ground_state_d5/2

# STATE 2 - Template for first excited state (FILL IN VALUES BELOW)
# Example: first excited state at E_x ~ 1.67 MeV
2 | 3.71 | 0 | 0.5 | 0 | 2.5 |2 | 19 | 8 | 8.0 | O19_first_excited_s1/2

# STATE 3 - Template for second excited state (FILL IN VALUES BELOW)
# Example: second excited state at E_x ~ 3.57 MeV
3 | 1.81 | 2 | 2.5 | 0 | 2.5 | 4 | 19 | 8 | 8.0 | O19_second_excited_d5/2

# NOTES:
# - Q-value = Q_gs - E_excitation (where Q_gs is ground state Q-value)
# - For O19(d,p)O20: Q_gs = 5.383 MeV
# - Uncomment state lines (remove leading #) and fill in correct values
# - You can add more states by copying the template format
# - target_spin should match the spin of the target nucleus in the outgoing channel
